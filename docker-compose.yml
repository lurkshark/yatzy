version: "3"
services:
  yatzy:
    image: node:12.16-stretch
    working_dir: /usr/yatzy
    ports:
    - "5001:5001"
    volumes:
    - ./:/usr/yatzy
    command: >
      sh -c "npm install &&
             npm run start"
